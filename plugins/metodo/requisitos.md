# metodo — qué necesita tu computadora

**Nada.** Las 4 skills (`/metodo:arrancar`, `/metodo:planear`, `/metodo:criticar`, `/metodo:cerrar`) son solo texto: funcionan con Claude Code, sin programas de fondo, sin claves, sin instalar nada.

Si igual querés ver el estado de tu equipo, corré el chequeo del catálogo (`docs/CHEQUEO.md`).
